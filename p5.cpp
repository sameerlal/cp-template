#include <iostream>
#include <algorithm>
#include <math.h>
#include <string.h>
#include <unordered_map>
#include <bits/stdc++.h>
#include <set>
#include <vector>
#include <bitset>
#define mp make_pair
#define pb push_back
#define fi first
#define se second
#define fill(x, y) memset(x, y, sizeof(y))
#define debug(x)                          \
    {                                     \
        cerr << #x << " = " << x << endl; \
    }
typedef long long ll;
const int MOD = 1e9 + 7;
using namespace std;
 
void solve()
{
    int n; cin >> n;
    ll arr[n];
    for (int i = 0;i<n;i++)
        cin >> arr[i];
}

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    // int t; t = 1; 
    int t; cin >> t;
    while (t--)
        solve();
}
